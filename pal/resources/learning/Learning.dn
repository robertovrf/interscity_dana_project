//uses rest.RESys
uses monitoring.Perception

interface Learning {
	//void learns(store RESys resys, int collectionTime, int explorationTreshold, int rounds)
	void learns(store Perception resys, int collectionTime, int explorationTreshold, int rounds)
}
